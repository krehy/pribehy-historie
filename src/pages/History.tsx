import { HistoryScroll } from "@/components/history/HistoryScroll";

/** Samostatná stránka „Historie světa" (dlouhý vertikální scroll) — /historie. */
export default function History() {
  return <HistoryScroll />;
}
